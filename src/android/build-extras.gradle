android {
    compileSdkVersion 29 // or 28 if you want; but 29+ is better for more APIs
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 29
    }
}

dependencies {
    // No androidx dependencies!
}