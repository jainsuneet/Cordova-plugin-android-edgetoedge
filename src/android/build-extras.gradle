android {
    compileSdkVersion 35  // must be >= 30 for WindowInsets.Type + setDecorFitsSystemWindows
    defaultConfig {
        minSdkVersion 23   // from your config.xml
        targetSdkVersion 35
    }
}